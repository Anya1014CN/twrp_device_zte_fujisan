diff --git a/gui/blanktimer.cpp b/gui/blanktimer.cpp
--- a/gui/blanktimer.cpp
+++ b/gui/blanktimer.cpp
@@ -31,6 +31,15 @@ blanktimer::blanktimer(void) {
 bool blanktimer::isScreenOff() {
 	return state >= kOff;
 }
+
+static const char* blank_state_name(int state) {
+	switch (state) {
+		case 0: return "kOn";
+		case 1: return "kDim";
+		case 2: return "kOff";
+		case 3: return "kBlanked";
+		default: return "unknown";
+	}
+}
 
 void blanktimer::setTime(int newtime) {
 	pthread_mutex_lock(&mutex);
@@ -46,6 +55,7 @@ void blanktimer::checkForTimeout() {
 	pthread_mutex_lock(&mutex);
 	timespec curTime, diff;
 	clock_gettime(CLOCK_MONOTONIC, &curTime);
+	LOGINFO("blanktimer::checkForTimeout state=%s timer=%d\n", blank_state_name(state), sleepTimer);
 	diff = TWFunc::timespec_diff(btimer, curTime);
 	if (sleepTimer > 2 && diff.tv_sec > (sleepTimer - 2) && state == kOn) {
 		orig_brightness = getBrightness();
@@ -78,6 +88,7 @@ string blanktimer::getBrightness(void) {
 }
 
 void blanktimer::resetTimerAndUnblank(void) {
+	LOGINFO("blanktimer::resetTimerAndUnblank state=%s\n", blank_state_name(state));
 #ifndef TW_NO_SCREEN_TIMEOUT
 	pthread_mutex_lock(&mutex);
 	setTimer();
@@ -108,6 +119,7 @@ void blanktimer::blank(void) {
  *     TW_NO_SCREEN_TIMEOUT and do not blank screen here either
  */
 
+	LOGINFO("blanktimer::blank state=%s\n", blank_state_name(state));
 #ifndef TW_NO_SCREEN_TIMEOUT
 	pthread_mutex_lock(&mutex);
 	if (state == kOn) {
@@ -124,6 +136,7 @@ void blanktimer::blank(void) {
 }
 
 void blanktimer::toggleBlank(void) {
+	LOGINFO("blanktimer::toggleBlank state=%s\n", blank_state_name(state));
 	if (state == kOn) {
 		blank();
 		PageManager::ChangeOverlay("lock");
diff --git a/gui/pages.cpp b/gui/pages.cpp
--- a/gui/pages.cpp
+++ b/gui/pages.cpp
@@ -1563,6 +1563,7 @@ void PageManager::SetStartPage(const std::string& page_name) {
 
 int PageManager::ChangePage(std::string name)
 {
+	LOGINFO("PageManager::ChangePage current=%s target=%s\n", GetCurrentPage().c_str(), name.c_str());
 	DataManager::SetValue("tw_operation_state", 0);
 	int ret = (mCurrentSet ? mCurrentSet->SetPage(name) : -1);
 	return ret;
@@ -1575,6 +1576,7 @@ std::string PageManager::GetCurrentPage()
 
 int PageManager::ChangeOverlay(std::string name)
 {
+	LOGINFO("PageManager::ChangeOverlay target=%s\n", name.c_str());
 	if (name.empty())
 		return mCurrentSet->SetOverlay(NULL);
 	else
diff --git a/minuitwrp/graphics.cpp b/minuitwrp/graphics.cpp
--- a/minuitwrp/graphics.cpp
+++ b/minuitwrp/graphics.cpp
@@ -458,6 +458,7 @@ int gr_fb_height(void)
 
 void gr_fb_blank(bool blank)
 {
+	LOGINFO("gr_fb_blank blank=%d\n", blank ? 1 : 0);
 	gr_backend->blank(gr_backend, blank);
 }
